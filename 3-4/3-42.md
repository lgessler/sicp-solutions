The two are the same, except I'd expect that Ben's version would be very
slightly more efficient, as the serialization is done exactly once, whereas in
the original version serialization is done on demand.
